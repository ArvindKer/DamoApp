//Imports
import { StyleSheet } from "react-native";

export default StyleSheet.create({

    txtinput_txt_style: {
        width: "80%",
        height: 50,
        borderWidth: 1,
        borderColor: '#fff',
        padding: 8,
        marginTop: 50,
        color: '#fff',
        borderRadius: 10,
        fontWeight: "500",
        fontSize: 16
    },
    submit_btn: {
        width: 200,
        height: 45,
        borderRadius: 20,
        backgroundColor: "#fff",
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 50
    },
    submit_btn_txt: {
        fontSize: 18,
        color: '#BDD63B',
        fontWeight: '600'
    }
});